# compose 
COMPOSE_PROJECT_NAME=ts_pg_test
# Project identification
PROJECT_NAME=av
ENVIRONMENT=dev  # or prod, staging, etc.

# Database Configuration
POSTGRES_PASSWORD=dev_pw
AV_USER_PASSWORD=dev_pw
DB_PORT=6433  # Non-standard port

# PgAdmin Configuration
PGADMIN_PASSWORD=admin_pw
PGADMIN_PORT=6051  # Non-standard port
PGADMIN_EMAIL=admin@unknown.com

# Application Database URL (using non-standard port)
DATABASE_URL=postgres://ts_user:dev_pw@localhost:6433/sec_master
